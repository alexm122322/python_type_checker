from setuptools import find_packages, setup

setup(
    name='type_checker',
    packages=find_packages(include=['type_checker']),
    version='0.1.0',
    description='Library for function type checking',
    author='oleksii.minaiev@postindustria.com',
)